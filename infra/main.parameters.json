{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "environmentName": {
        "value": "${AZURE_ENV_NAME}"
      },
      "location": {
        "value": "${AZURE_LOCATION}"
      },
      "azurePrincipalId": {
        "value": "${AZURE_PRINCIPAL_ID}"
      },
      "checkoutApiImageName": {
        "value": "${CHECKOUT_API_IMAGE_NAME}"
      },
      "menuApiImageName": {
        "value": "${MENU_API_IMAGE_NAME}"
      },
      "webAppImageName": {
        "value": "${WEB_APP_IMAGE_NAME}"
      }
    }
  }